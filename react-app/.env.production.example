# ─── Ansar Mahmood — React SPA (ansarmahmood.org) PRODUCTION ───
# Copy to `.env.production` before building:  npm run build
# (Vite auto-loads .env.production for production builds.)

# Served at the web root, so base is "/"
VITE_BASE=/

# The Laravel API on its subdomain
VITE_API_BASE=https://api.ansarmahmood.org/api
